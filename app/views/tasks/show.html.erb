<h2><%=@task.title%></h2>
<p><%=@task.details%></p>
<p><%=@task.completed%></p>


<%= link_to "Edit task", edit_path %>
<!-- link_to "Fed up delete it!", destroy_path renvoi sur un get -->
<%= link_to "Back to list", tasks_path %>
<%= link_to "Fed up delete with #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
